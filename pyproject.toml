[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cif-parse"
version = "0.1.0"
description = "Utilities for parsing and annotating mmCIF structures."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "biotite>=1.6,<2",
    "numpy>=2.3,<3",
    "biopython>=1.85,<2",
    "pyhmmer>=0.11,<1",
    "tqdm>=4.67,<5",
]

[project.optional-dependencies]
analysis = [
    "pandas>=2.3,<3",
    "scipy>=1.17,<2",
    "networkx>=3.6,<4",
]
immune = [
    "sadie-antibody>=2.0,<3",
]
chem = [
    "rdkit>=2026.03",
]
dev = [
    "pytest>=8,<9",
]
all = [
    "pandas>=2.3,<3",
    "scipy>=1.17,<2",
    "networkx>=3.6,<4",
    "sadie-antibody>=2.0,<3",
    "rdkit>=2026.03",
    "pytest>=8,<9",
    "biopython>=1.85,<2",
    "pyhmmer>=0.11,<1",
]

[project.scripts]
cif-parse = "cif_parse.cli:main"
cif-parse-cluster = "cif_parse.clustering.cli:main"
cif-parse-cluster-monomers = "cif_parse.clustering.cli:main"
cif-parse-refine = "cif_parse.refine.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
